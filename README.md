# Train BDT

1. Get the repository

    ```bash
    git clone git@github.com:ram1123/Run2_MVA_trainer.git
    cd Run2_MVA_trainer
    ```

2. Update the parameters in file [train.sh](train.sh). For example: `year`, `label` and `input path of stage-1`

    bash train.sh
